## Amendments to draft plan, by step/substep:



**Step 2**

- Remove any instructions that create commentary "findings", instead present tables (showing counts) and plots (showing stacked bar charts) of all the main quantities. Accompanying text should show any summary statistic not included in the tables or plots, and relevant information and definitions e.g. definitions of phases B1, B2, B3, data sparsity, signal exhaustion, etc.
- Organise plots and tables so they compare the cold and warm audits, not the groups, e.g. stacked bar chart of cold and warm for each of: constant speed machines; CAZ+; Rest of London; variable speed. Create two sets of these plots: with X (time) axis of year; with X axis comprising phases {A1, A2, B1, B2, B3, 2025, 2026}.
- 2.1 
  - Correct Step 2.1 based on revised approach to variable_speed group, remove treatment of "structural NAs" that inserts NAs where there is no in-scope data, instead flag these in accompanyting text.
  - Perform checksum analysis across all sub-groups to ensure all datapoints are accounted for, and tabulate for user inspection.
  - Organise plots and tables so they compare the cold and warm audits, not the groups, e.g. stacked bar chart of cold and warm for each of: constant speed machines; CAZ+; Rest of London; variable speed. Create two sets of these plots: with X (time) axis of year; with X axis comprising phases {A1, A2, B1, B2, B3, 2025, 2026}.
  - Remove any instructions that create separate stacked plots of Phases B and C.
- 2.2
  - Create tables and a stacked bar chart plot showing the compliance outcomes variation with time for cold and warm, across each group.
  - Create a separate table and plot focussed on binary compliance/non-compliance only, using the same X and data group categories as Step 2.1.
- 2.3
  - Tabulate the data sparsity, signal exhaustion, and volatility data.
- 2.4
  - Analyse tables for implications of these statistics for successful WLS analysis in later steps.



